server:
 port: 8888

app:
  poll-interval: 10000

spring:
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
  main:
    banner-mode: "off"
---
spring:  
 datasource:
  driverClassName: oracle.jdbc.driver.OracleDriver
  url: jdbc:oracle:...
  username: --
  password: --